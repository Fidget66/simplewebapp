
spring:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: validate
    database: postgresql
    generate-ddl: false
  sql:
    init:
      mode: always
kafka:
  server: http://localhost:9092
  topic: email1
  group:
    id: myKafkaGroupId